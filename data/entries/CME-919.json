{
  "cme_id": "CME-919",
  "control_name": "JIT Compiler Hardening (JIT Restriction / V8 Sandbox)",
  "description": "Restricts or hardens Just-In-Time compilation in language runtimes and browser engines to prevent type confusion exploitation. Encompasses three postures: (1) JIT disabling \u2014 turning off JIT compilation entirely via flags (--jitless in V8, javascript.options.ion in Firefox) which eliminates JIT-spraying and JIT-based type confusion at the cost of performance; (2) V8 Sandbox enforcement \u2014 a hardware-backed memory cage that isolates JIT-compiled code and V8 heap objects from the rest of the process address space, requiring an additional sandbox escape even after achieving type confusion; (3) JIT tiering restrictions \u2014 limiting optimization tiers to reduce the surface of speculative type assumptions that can be violated. Applicable to Chromium-based browsers, Node.js, Electron applications, and SpiderMonkey-based runtimes. Complements CME-113 (CFI/Shadow Call Stack) which prevents type-confused pointers from redirecting control flow in native code.",
  "tactic": "Harden",
  "category": "Application Controls",
  "category_id": "application-controls",
  "function_id": "runtime-hardening",
  "control_layer": "Application",
  "attenuation_type": "deterministic",
  "cvss_vector_impacts": [
    {
      "metric": "AC",
      "from": "L",
      "to": "H",
      "rationale": "JIT disabling eliminates the type speculation attack surface entirely; V8 Sandbox requires the attacker to chain a sandbox escape after the type confusion, converting a single-step exploit into a multi-step chain that must defeat both the type system and the memory cage"
    },
    {
      "metric": "S",
      "from": "C",
      "to": "U",
      "rationale": "V8 Sandbox confines the impact of type confusion to the sandbox boundary; the attacker cannot directly reach renderer process memory or OS resources without an additional escape primitive",
      "conditions": [
        "Requires V8 Sandbox posture specifically — JIT disabling (posture 1) eliminates exploitation entirely making scope moot, but JIT tiering restrictions alone (posture 3) do not add a scope boundary"
      ]
    }
  ],
  "cwe_relationships": [
    "CWE-843",
    "CWE-119",
    "CWE-787"
  ],
  "verification": {
    "method": "Verify JIT hardening is enabled in browser or runtime configuration. Check for jitless mode, V8 Sandbox flags, or JIT disabling in Firefox preferences.",
    "commands": [
      {
        "command": "grep -r 'jitless\\|--js-flags.*--jitless' /etc/chromium/ /opt/google/chrome/ ~/.config/chromium/ 2>/dev/null",
        "expected": "Non-empty output — --jitless flag found in Chromium launch configuration",
        "platform": "linux"
      },
      {
        "command": "grep -r 'javascript.options.ion' /usr/lib64/firefox/defaults/pref/ ~/.mozilla/firefox/*/prefs.js 2>/dev/null",
        "expected": "Non-empty output — javascript.options.ion preference found (should be set to false)",
        "platform": "linux"
      },
      {
        "command": "node --v8-flags=--help 2>&1 | grep -i sandbox",
        "expected": "Non-empty output — V8 sandbox flags available in the Node.js runtime",
        "platform": "any"
      },
      {
        "command": "reg query \"HKLM\\SOFTWARE\\Policies\\Microsoft\\Edge\" /v JITDisabled 2>nul & reg query \"HKLM\\SOFTWARE\\Policies\\Google\\Chrome\" /v JITDisabled 2>nul",
        "expected": "JITDisabled    REG_DWORD    0x1",
        "platform": "windows"
      }
    ]
  },
  "confidence": "High",
  "platforms": [
    "Any"
  ],
  "scf_identifiers": [
    "SEA-10",
    "WEB-01.1"
  ]
}
