# Coverage assessments

Coverage assessments record the evidence behind a conclusion that CME coverage exists, is inadequate, is unknown, or does not exist. A negative conclusion must enumerate the reviewed candidate controls and why each was rejected; absence of a matching entry is not evidence.

Files must validate against `schema/coverage-assessment.schema.json` and use the `CMA-<ID>` filename convention.
