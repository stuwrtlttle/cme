{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cmetaxonomy.org/schema/cme-entry.schema.json",
  "title": "Common Mitigation Enumeration (CME) Entry",
  "description": "A structured, verifiable defensive control entry that records its effect, evidence state, relationships, and optional framework bindings.",
  "type": "object",
  "required": ["cme_id", "control_name", "description", "tactic", "category", "category_id", "effect_mode", "evidence_state"],
  "if": {
    "not": { "required": ["function_id"] }
  },
  "then": {
    "required": ["control_layer"]
  },
  "dependentRequired": {
    "cve_affected": ["cve_schema_version"]
  },
  "allOf": [
    {
      "if": { "properties": { "evidence_state": { "const": "deterministic" } } },
      "then": { "required": ["verification"], "properties": { "effect_mode": { "const": "preventive" } } }
    },
    {
      "if": { "properties": { "evidence_state": { "const": "quantified" } } },
      "then": { "required": ["efficacy"], "properties": { "efficacy": { "required": ["probability", "evidence_basis", "conditions"] } } }
    },
    {
      "if": { "properties": { "evidence_state": { "const": "unquantified" } } },
      "then": { "required": ["efficacy"] }
    }
  ],
  "properties": {
    "cme_id": {
      "type": "string",
      "pattern": "^CME-\\d{3,4}$",
      "description": "Unique CME identifier (e.g., CME-104, CME-1001)"
    },
    "control_name": {
      "type": "string",
      "description": "Formal name of the mitigation control"
    },
    "description": {
      "type": "string",
      "description": "Technical description of what the control does and how it mitigates risk"
    },
    "tactic": {
      "type": "string",
      "enum": ["Harden", "Isolate", "Detect", "Evict", "Restore"],
      "description": "D3FEND-aligned tactic category"
    },
    "category": {
      "type": "string",
      "description": "Sub-category within the tactic (e.g., Kernel Hardening, Network Isolation)"
    },
    "category_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Kebab-case identifier linking to the category registry in data/categories.json"
    },
    "function_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Links to a function in data/functions.json. Omit for standalone controls."
    },
    "control_layer": {
      "type": "string",
      "enum": ["Network", "OS/Kernel", "Application", "Data", "Identity"],
      "description": "Technology stack layer where the control resides. Required when function_id is absent; inherited from function when present."
    },
    "effect_mode": {
      "type": "string",
      "enum": ["preventive", "detective", "corrective"],
      "description": "Primary control effect: prevent an attack, detect it, or correct its consequences."
    },
    "evidence_state": {
      "type": "string",
      "enum": ["deterministic", "quantified", "unquantified"],
      "description": "Evidence maturity. Deterministic effects are automatically credited only when verified; quantified effects require measured efficacy; unquantified effects are surfaced but never automatically credited."
    },
    "efficacy": {
      "type": "object",
      "properties": {
        "probability": { "type": "number", "minimum": 0, "maximum": 1 },
        "evidence_basis": { "type": "string", "minLength": 1 },
        "conditions": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } }
      },
      "required": ["conditions"],
      "allOf": [
        {
          "if": { "required": ["probability"] },
          "then": { "required": ["evidence_basis"] }
        }
      ],
      "description": "Evidence and conditions for quantified or unquantified control efficacy. Quantified entries require probability and evidence_basis."
    },
    "attenuation_type": {
      "type": "string",
      "enum": ["deterministic", "probabilistic"],
      "deprecated": true,
      "description": "Legacy compatibility field. Use evidence_state."
    },
    "cvss_vector_impacts": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/cvss_vector_impact"
      },
      "description": "Optional CVSS environmental metric modifications. CVSS is a framework binding, not a CME membership requirement."
    },
    "cwe_relationships": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^CWE-\\d+$"
      },
      "description": "Legacy compatibility index of CWE IDs. Use relationships for typed mappings."
    },
    "relationships": {
      "type": "array",
      "items": { "$ref": "#/$defs/relationship" },
      "description": "Typed relationships to CWE, controls, or external-framework objects."
    },
    "framework_bindings": {
      "type": "array",
      "items": { "$ref": "#/$defs/framework_binding" },
      "description": "Optional bindings to external frameworks such as CVSS, SSVC, ATT&CK, ATLAS, ICS, D3FEND, and SCF."
    },
    "scf_identifiers": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[A-Z]{2,4}-\\d{2}(\\.\\d+)?$"
      },
      "description": "Secure Controls Framework (SCF) control identifiers this CME entry satisfies (e.g., CRY-03, NET-06.1)"
    },
    "verification": {
      "type": "object",
      "properties": {
        "method": {
          "type": "string",
          "description": "Human-readable description of how to verify the control is active"
        },
        "commands": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "command": {
                "type": "string",
                "description": "Shell command to run"
              },
              "expected": {
                "type": "string",
                "description": "Expected output or pattern indicating the control is active"
              },
              "platform": {
                "type": "string",
                "enum": ["linux", "rhel", "debian", "kubernetes", "windows", "macos", "any"],
                "description": "Platform this check applies to"
              }
            },
            "required": ["command", "expected"]
          },
          "description": "Machine-executable verification checks"
        }
      },
      "description": "How a scanner or agent can confirm the control is active"
    },
    "d3fend_mapping": {
      "type": "object",
      "properties": {
        "technique_id": {
          "type": "string",
          "description": "D3FEND technique ID (e.g., D3-SPP)"
        },
        "technique_name": {
          "type": "string",
          "description": "D3FEND technique name"
        }
      },
      "description": "Mapping to equivalent D3FEND technique if one exists"
    },
    "references": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "source": { "type": "string" },
          "url": { "type": "string", "format": "uri" },
          "section": { "type": "string" }
        },
        "required": ["source"]
      },
      "description": "External references (Red Hat docs, NIST, CIS benchmarks, etc.)"
    },
    "confidence": {
      "type": "string",
      "enum": ["High", "Medium", "Low"],
      "deprecated": true,
      "description": "Legacy editorial confidence. Use evidence_state and evidence fields for scoring."
    },
    "platforms": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Legacy platform applicability list. Use cve_affected for structured product identity with CPE/PURL support."
    },
    "cve_schema_version": {
      "type": "string",
      "pattern": "^5\\.(0|[1-9][0-9]*)(\\.(0|[1-9][0-9]*))?$",
      "description": "CVE Record Format schema version that cve_affected conforms to (e.g., '5.1', '5.2.0'). Pinned explicitly so parsers don't have to detect it. Required when cve_affected is present."
    },
    "cve_affected": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/cme_product"
      },
      "description": "Products and platforms where this control is applicable, using the CVE 5.x affected[] container shape. Enables structural joins against CVE records for product-scoped mitigation queries."
    }
  },
  "$defs": {
    "relationship": {
      "type": "object",
      "required": ["target_namespace", "target_id", "relationship_type"],
      "properties": {
        "target_namespace": { "type": "string", "minLength": 1 },
        "target_id": { "type": "string", "minLength": 1 },
        "relationship_type": { "type": "string", "enum": ["mitigates", "supports", "supported_by", "identical", "equivalent", "contrary", "no_relationship", "requires", "enables", "strengthens"] },
        "method": { "type": "string", "enum": ["supportive", "structural", "crosswalk", "set_theory"] },
        "properties": { "type": "array", "items": { "type": "string", "enum": ["example_of", "integral_to", "precedes"] } },
        "rationale": { "type": "string" },
        "evidence_state": { "type": "string", "enum": ["deterministic", "quantified", "unquantified"] }
      }
    },
    "framework_binding": {
      "type": "object",
      "required": ["namespace", "identifier", "relationship_type"],
      "properties": {
        "namespace": { "type": "string", "enum": ["CVSS", "SSVC", "ATT&CK", "ATLAS", "ICS", "D3FEND", "SCF"] },
        "version": { "type": "string" },
        "identifier": { "type": "string", "minLength": 1 },
        "value": { "type": "string" },
        "relationship_type": { "type": "string", "enum": ["maps_to", "mitigates", "supports", "informs"] },
        "rationale": { "type": "string" },
        "conditions": { "type": "array", "items": { "type": "string" } },
        "evidence_basis": { "type": "string" }
      }
    },
    "cvss_vector_impact": {
      "type": "object",
      "required": ["metric", "from", "to"],
      "properties": {
        "metric": {
          "type": "string",
          "enum": ["AV", "AC", "PR", "UI", "S", "C", "I", "A"],
          "description": "CVSS base metric being modified"
        },
        "from": {
          "type": "string",
          "description": "Original metric value (e.g., N, L, H, C)"
        },
        "to": {
          "type": "string",
          "description": "Modified metric value when control is active"
        },
        "rationale": {
          "type": "string",
          "description": "Technical justification for this specific attenuation"
        },
        "probability": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Estimated likelihood that this metric shift applies when the control is active. Only meaningful for probabilistic controls."
        },
        "evidence_basis": {
          "type": "string",
          "description": "Citation or source for the probability estimate (vendor benchmark, published study, testing methodology)"
        },
        "conditions": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Prerequisites for the probability estimate to hold (specific rule set versions, configuration requirements, etc.)"
        }
      }
    },
    "cme_status": {
      "type": "string",
      "enum": ["applicable", "not-applicable", "unknown"],
      "description": "Whether this control is applicable to the product. Analogous to CVE status (affected/unaffected/unknown) but with CME-specific semantics: 'applicable' means the control can be deployed on this product."
    },
    "cme_version": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1024,
      "description": "A version string, following the CVE 5.x version definition."
    },
    "cme_product": {
      "$comment": "Mirrors CVE Record Format 5.2.0 #/definitions/product. Status enum changed from affected/unaffected/unknown to applicable/not-applicable/unknown. See https://github.com/CVEProject/cve-schema",
      "type": "object",
      "anyOf": [
        { "required": ["vendor", "product"] },
        { "required": ["collectionURL", "packageName"] }
      ],
      "properties": {
        "vendor": {
          "type": "string",
          "minLength": 1,
          "maxLength": 512,
          "description": "Name of the organization that created or maintains this product"
        },
        "product": {
          "type": "string",
          "minLength": 1,
          "maxLength": 2048,
          "description": "Name of the product where this control is applicable"
        },
        "collectionURL": {
          "type": "string",
          "format": "uri",
          "minLength": 1,
          "maxLength": 2048,
          "description": "URL identifying a package collection (determines the meaning of packageName)"
        },
        "packageName": {
          "type": "string",
          "minLength": 1,
          "maxLength": 2048,
          "description": "Name or identifier of the package as used in the package collection"
        },
        "packageURL": {
          "type": "string",
          "format": "uri",
          "minLength": 1,
          "maxLength": 2048,
          "description": "Package URL (PURL) for the product, without version (e.g., pkg:rpm/redhat/kernel)"
        },
        "cpes": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 2048,
            "pattern": "([c][pP][eE]:/[AHOaho]?(:[A-Za-z0-9._\\-~%]*){0,6})|(cpe:2\\.3:[aho*\\-](:[A-Za-z0-9._\\-~%*]*){0,12})"
          },
          "description": "CPE identifiers for this product (CPE 2.2 or 2.3 format)"
        },
        "modules": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 4096
          },
          "description": "Affected components, features, modules, or sub-components"
        },
        "platforms": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "maxLength": 1024
          },
          "description": "Platforms where this control applies to this product (e.g., x86_64, ARM, 64 bit). Absent means all platforms."
        },
        "programFiles": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 1024
          },
          "description": "Source code files relevant to this control on this product"
        },
        "programRoutines": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "object",
            "required": ["name"],
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1,
                "maxLength": 4096
              }
            },
            "additionalProperties": false
          },
          "description": "Program routines relevant to this control"
        },
        "repo": {
          "type": "string",
          "format": "uri",
          "description": "URL of the source code repository"
        },
        "defaultStatus": {
          "$ref": "#/$defs/cme_status",
          "description": "Default applicability status for versions not listed in the versions array. Defaults to 'unknown' if omitted."
        },
        "versions": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "object",
            "required": ["version", "status"],
            "properties": {
              "version": {
                "$ref": "#/$defs/cme_version",
                "description": "The single version being described, or the start of a version range"
              },
              "status": {
                "$ref": "#/$defs/cme_status",
                "description": "Applicability status for this version or range"
              },
              "versionType": {
                "type": "string",
                "minLength": 1,
                "maxLength": 128,
                "description": "The version numbering system (e.g., semver, rpm, custom, git)"
              },
              "lessThan": {
                "$ref": "#/$defs/cme_version",
                "description": "Non-inclusive upper limit of the version range"
              },
              "lessThanOrEqual": {
                "$ref": "#/$defs/cme_version",
                "description": "Inclusive upper limit of the version range"
              },
              "changes": {
                "type": "array",
                "minItems": 1,
                "uniqueItems": true,
                "items": {
                  "type": "object",
                  "required": ["at", "status"],
                  "properties": {
                    "at": {
                      "$ref": "#/$defs/cme_version",
                      "description": "The version at which a status change occurs"
                    },
                    "status": {
                      "$ref": "#/$defs/cme_status",
                      "description": "The new applicability status starting at this version"
                    }
                  },
                  "additionalProperties": false
                },
                "description": "Status changes within the version range"
              }
            }
          },
          "description": "Version ranges where this control is applicable"
        }
      }
    }
  }
}
